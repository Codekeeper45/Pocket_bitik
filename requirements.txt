telethon==1.43.2
openai==2.38.0
python-dotenv==1.2.2
requests==2.33.1
tiktoken  # опционально: точный подсчёт токенов для обрезки контекста; при отсутствии — фоллбэк на оценку по символам

# Системная зависимость (не ставится через pip):
#   ffmpeg (с libopus) — нужен для: (1) извлечения аудио из видео-кружков (.ask с video_note)
#   и (2) кодирования голосовых ответов PCM→OGG/Opus (.ask -v / .voice, Gemini TTS).
#   Установить на сервере: apt install ffmpeg / dnf install ffmpeg
# Голосовые ответы (Gemini TTS) идут через REST (requests) — отдельный pip-пакет НЕ нужен.
