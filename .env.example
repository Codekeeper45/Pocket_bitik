# ════════════════════════════════════════════════════════════════
#  НАСТРОЙКИ БОТА
#  Скопируйте этот файл в .env и подставьте свои значения.
#  Команда для копирования:  cp .env.example .env   (Windows: copy .env.example .env)
#  НИКОМУ не показывайте готовый .env — это ваши ключи и доступы!
# ════════════════════════════════════════════════════════════════

# --- Telegram (ОБЯЗАТЕЛЬНО) ---
# Получить на https://my.telegram.org → API development tools
api_id=0000000
api_hash=ваш_api_hash_сюда

# --- ИИ-ключ для ответов (минимум — ОДИН ключ DeepSeek) ---
# DeepSeek — это «мозг» бота по умолчанию. Только этого ключа достаточно,
# чтобы бот запустился и работали .ask / .search / .digest / авто-ответ.
# Получить ключ: https://platform.deepseek.com
DEEPSEEK_API_KEY=sk-...
DEEPSEEK_MODEL=deepseek-v4-pro

# ════════════════════════════════════════════════════════════════
#  НЕОБЯЗАТЕЛЬНЫЕ ключи ниже. Без них бот НЕ упадёт — просто
#  соответствующие функции работать не будут (см. таблицу в README).
#  Если ключа нет — оставьте поле пустым или удалите строку.
# ════════════════════════════════════════════════════════════════

# OpenRouter (необязательно) — нужен для распознавания картинок, голосовых
# и видео-кружков внутри .ask, а также для ГЕНЕРАЦИИ изображений командой /gen
# (бесплатная модель Riverflow V2.5 Pro). Без него медиа просто не читаются,
# текст чата при этом разбирается нормально. Ключ: https://openrouter.ai/keys
OPENROUTER_API_KEY=

# OpenCode (необязательно) — нужен ТОЛЬКО если вы хотите переключаться
# на модели Kimi / GLM / Qwen / MiniMax / MiMo / Hunyuan через .model.
OPENCODE_API_KEY=

# OpenAI (необязательно) — официальный API, даёт модели GPT-5.5 / GPT-5.4 / o3 /
# GPT-5.4 Mini / o4-mini для ответов (.model → раздел «OpenAI»). Нужен баланс на platform.openai.com.
# Ключ: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# z.ai / Zhipu (необязательно) — модели GLM (GLM-5.2 / GLM-4.7 Flash / GLM-4.6V Flash)
# для ответов (.model → раздел «z.ai (GLM)»). GLM-4.6V Flash видит картинки (-g);
# Flash-модели на z.ai бесплатны. Ключ (формат id.secret): https://z.ai (API Keys).
ZAI_API_KEY=

# Fireworks AI (необязательно) — serverless-модели (MiniMax M3 / Nemotron 3 Ultra /
# DeepSeek V4 Pro / GLM-5.2 / Kimi K2.6) для ответов (.model → раздел «Fireworks»).
# MiniMax M3 и Kimi K2.6 видят картинки (-g). Ключ (формат fw_…): https://fireworks.ai (API Keys).
FIREWORKS_API_KEY=

# Sakana AI / Fugu (необязательно) — оркестратор поверх фронтир-LLM (.model → раздел
# «Sakana AI (Fugu)»). Модели: Fugu (быстрая) и Fugu Ultra (мульти-агентная, сильная, но
# может быть медленной). Обе видят картинки (-g), окно 1M. Ключ: https://console.sakana.ai.
SAKANA_API_KEY=
# SAKANA_PROXY (необязательно) — если эдж Sakana отдаёт 403 Forbidden с IP сервера (WAF режет
# датацентры/хостинги по ASN), пусти запросы Sakana через прокси с «чистым» IP. Формат:
# http://[user:pass@]host:port  или  socks5://[user:pass@]host:port. Касается ТОЛЬКО Sakana.
SAKANA_PROXY=

# ModelGate (необязательно) — OpenAI-совместимый шлюз к моделям Claude
# (Opus / Sonnet / Haiku). Появляются в .model разделом «Claude (ModelGate)».
# Текст и поиск по каналам работают; картинки напрямую (-g) шлюз не передаёт.
# Ключ: https://modelgate.app/console/token
MODELGATE_API_KEY=

# Google GenAI / Gemini (необязательно) — даёт текстовые модели Gemini 3.5 Flash /
# 3.1 Flash Lite для ответов (.model → раздел «Google Gemini»; видят картинки -g) И
# голосовые ответы (.voice + флаг -v). Один ключ на оба. Без него — текстом, без Gemini.
# Ключ: https://aistudio.google.com/apikey
# Можно указать несколько ключей через запятую (ротация при лимитах) —
# здесь или в GOOGLE_GENAI_API_KEYS.
GOOGLE_GENAI_API_KEY=
GOOGLE_GENAI_API_KEYS=

# Fish Audio TTS (необязательно) — альтернативный движок озвучки с большим выбором
# голосов. Переключение: .voice engine fish · поиск/избранное: .voice fish.
# Ключ: https://fish.audio (раздел API). Модель по умолчанию s1 (можно s2-pro).
FISH_AUDIO_API_KEY=
FISH_TTS_MODEL=s1

# LlamaCloud (необязательно) — дешёвый OCR фото в /ask по умолчанию (LlamaParse,
# tier cost-effective): с картинок берётся текст. Флаг -m возвращает vision-описания.
# Без ключа фото автоматически описывает vision-модель (как раньше).
# Ключ: https://cloud.llamaindex.ai (API Keys).
LLAMA_CLOUD_API_KEY=

# Tavily (необязательно) — ВЕБ-ПОИСК для /ask: модель сама решает, когда искать
# в интернете (web_search), читать страницы по ссылкам (web_extract), обходить
# сайты (web_crawl) и строить карту сайта (web_map). Бесплатно 1000 запросов/мес.
# Ключ: https://app.tavily.com
TAVILY_API_KEY=
